const user={
    firstName:"Kaif",
    lastName:"Bhombal",
    age:25
}

function handleObject(anyobject){
    console.log(`Username  intital is ${anyobject.firstName} and last name is ${anyobject.lastName} and age is ${anyobject.age}`);
}
handleObject(user)
